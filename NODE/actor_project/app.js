
const { name } = require("./actor1")
const { age } = require("./actor2")
const { description } = require("./actor3")

console.log(
    `My name is ${name} and my age is ${age} & this is about me: ${description}`
);